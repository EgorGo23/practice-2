БИБЛИОТЕКА UTIL

Чтобы воспользоваться библиотекой UTIL подключите script на свою страницу.
Далее, можно вызвать интересующую вас функцию, как свойство объекта util. 

Для проверки значения:
    -isString
    -isNumber
    -isNan
    -isBool
    -isNull
    -isUndef
    -isSymb
    -isObj

Для привидения к типу:
    -toNumber
    -toStr
    -toBool
    -toObj

Пример:
    util.isBool(1) - в консоле будет false,
    util.isBool(true) - в консоле будет true.



ТЕСТИРОВАНИЕ

Для тестирования библиотеки util разработана библиотека utilTests. Чтобы произвести тестирование, необходимо подключить
модуль utilTests после модуля util и вызвать функцию тестирования(без аргументов), как свойство объекта utilTests.

Пример: 
    utilTests.isString() - в консоле появится вывод тестов для функции isString() с различными аргументами.